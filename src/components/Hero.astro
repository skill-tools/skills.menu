---
---

<section class="hero">
<div class="container">
	<div class="badge">v0.1.0 · agentskills.io open standard</div>
	<h1>Build, validate, and route<br /><span class="highlight">agent skills</span></h1>
	<p class="hero-desc">
		The complete toolkit for SKILL.md — the portable, vendor-neutral
		format for packaging AI agent capabilities. Zero LLM dependencies.
	</p>
	<div class="hero-actions">
		<a href="https://github.com/skill-tools/skill-tools" target="_blank" rel="noopener" class="btn btn-primary">npm install -g skill-tools</a>
		<a href="/docs/core" class="btn">Read the Docs →</a>
	</div>
</div>
</section>

<style>
	.hero {
		padding: 5rem 0 3.5rem;
		text-align: center;
	}

	.badge {
		display: inline-block;
		font-size: 0.7rem;
		color: var(--blue);
		background: var(--blue-bg);
		border: 1px solid var(--blue-border);
		padding: 0.2rem 0.7rem;
		border-radius: 100px;
		margin-bottom: 1.5rem;
	}

	h1 {
		font-size: 2.25rem;
		font-weight: 700;
		letter-spacing: -0.03em;
		line-height: 1.3;
		margin-bottom: 1.25rem;
	}

	h1 .highlight { color: var(--blue); }

	.hero-desc {
		font-family: var(--font-body);
		font-size: 0.95rem;
		color: var(--muted);
		max-width: 520px;
		margin: 0 auto 2.5rem;
		line-height: 1.7;
	}

	.hero-actions {
		display: flex;
		justify-content: center;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	@media (max-width: 768px) {
		.hero { padding: 3.5rem 0 2.5rem; }
		h1 { font-size: 1.6rem; }
		.hero-actions { flex-direction: column; align-items: center; }
	}
</style>
