---
---

<section class="hero">
<div class="container">
	<div class="badge">v0.2.0 · agentskills.io spec</div>
	<h1>The <span class="highlight">SKILL.md</span> toolchain</h1>
	<div class="hero-packages">
		<div class="pkg"><strong>core</strong><span>parse &amp; validate</span></div>
		<div class="pkg"><strong>cli</strong><span>lint &amp; score</span></div>
		<div class="pkg"><strong>router</strong><span>BM25 retrieval</span></div>
		<div class="pkg"><strong>gen</strong><span>scaffold from text or OpenAPI</span></div>
	</div>
	<div class="hero-actions">
		<a href="https://github.com/skill-tools/skill-tools" target="_blank" rel="noopener" class="btn btn-primary">npm i -g skill-tools</a>
		<a href="/try" class="btn">Try it →</a>
	</div>
</div>
</section>

<style>
	.hero {
		padding: 5rem 0 3.5rem;
		text-align: center;
	}

	.badge {
		display: inline-block;
		font-size: 0.7rem;
		color: var(--blue);
		background: var(--blue-bg);
		border: 1px solid var(--blue-border);
		padding: 0.2rem 0.7rem;
		border-radius: 100px;
		margin-bottom: 1.5rem;
	}

	h1 {
		font-size: 2.25rem;
		font-weight: 700;
		letter-spacing: -0.03em;
		line-height: 1.3;
		margin-bottom: 1.25rem;
	}

	h1 .highlight { color: var(--blue); }

	.hero-desc {
		font-family: var(--font-body);
		font-size: 0.95rem;
		color: var(--muted);
		max-width: 520px;
		margin: 0 auto 2.5rem;
		line-height: 1.7;
	}

	.hero-packages {
		display: grid;
		grid-template-columns: repeat(4, auto);
		justify-content: center;
		gap: 0.5rem 1.5rem;
		margin: 0 auto 2.5rem;
	}

	.pkg {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.25rem;
	}

	.pkg strong {
		font-family: var(--font-mono, 'SF Mono', Monaco, monospace);
		font-size: 0.85rem;
		color: var(--fg);
	}

	.pkg span {
		font-family: var(--font-body);
		font-size: 0.75rem;
		color: var(--muted);
	}

	.hero-actions {
		display: flex;
		justify-content: center;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	@media (max-width: 768px) {
		.hero { padding: 3.5rem 0 2.5rem; }
		h1 { font-size: 1.6rem; }
		.hero-packages { grid-template-columns: repeat(2, auto); }
		.hero-actions { flex-direction: column; align-items: center; }
	}
</style>
