---
---

<section class="hero">
<div class="container">
	<div class="badge">v0.2.0 · agentskills.io spec</div>
	<h1>Lint, score, and route<br /><span class="highlight">SKILL.md</span> files</h1>
	<p class="hero-desc">
		Parse, validate, lint, and score SKILL.md files.
		Route queries to the right skill with BM25. No API keys, no LLMs needed.
	</p>
	<div class="hero-actions">
		<a href="https://github.com/skill-tools/skill-tools" target="_blank" rel="noopener" class="btn btn-primary">npm i -g skill-tools</a>
		<a href="/docs/core" class="btn">Docs</a>
		<a href="/try" class="btn">Try it →</a>
	</div>
</div>
</section>

<style>
	.hero {
		padding: 5rem 0 3.5rem;
		text-align: center;
	}

	.badge {
		display: inline-block;
		font-size: 0.7rem;
		color: var(--blue);
		background: var(--blue-bg);
		border: 1px solid var(--blue-border);
		padding: 0.2rem 0.7rem;
		border-radius: 100px;
		margin-bottom: 1.5rem;
	}

	h1 {
		font-size: 2.25rem;
		font-weight: 700;
		letter-spacing: -0.03em;
		line-height: 1.3;
		margin-bottom: 1.25rem;
	}

	h1 .highlight { color: var(--blue); }

	.hero-desc {
		font-family: var(--font-body);
		font-size: 0.95rem;
		color: var(--muted);
		max-width: 520px;
		margin: 0 auto 2.5rem;
		line-height: 1.7;
	}

	.hero-actions {
		display: flex;
		justify-content: center;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	@media (max-width: 768px) {
		.hero { padding: 3.5rem 0 2.5rem; }
		h1 { font-size: 1.6rem; }
		.hero-actions { flex-direction: column; align-items: center; }
	}
</style>
